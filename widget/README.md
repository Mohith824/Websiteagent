# Widget Directory

This directory will contain the front-end chat widget:
- Plain JavaScript using the browser's Shadow DOM.
- Standalone CSS styling encapsulated from host websites.
- Embeddable via a single `<script>` tag.
