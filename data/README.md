# Data Directory

This directory stores:
- `college_facts.json`: The source of truth for college information (programs, KCET/COMEDK codes, placements).
- `admissions.db`: The local SQLite database for captured student leads and conversation logs (ignored by git).
